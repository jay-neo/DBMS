-- ALTER TABLE emp_new
-- SET UNUSED comm;

-- MySQL does not support the concept of marking a column as unused.
-- You can drop the column if it's no longer needed or simply ignore it in your queries.
